{% extends 'base.html.twig' %}

{% block title %}Admin - Modifier serveur{% endblock %}

{% block body %}
    {# retour liste users#}
    <div class="row justify-content-start">
        <a href="{{ path('admin-List-Serveurs') }}" ><i class="fas fa-angle-left"></i> retour à la liste des serveurs</a>
    </div>
    <br>
    <h2>Admin - Modifier</h2>

    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.Modifier) }}
    {{ form_end(form) }}

{% endblock %}