{% extends 'base.html.twig' %}

{% block title %}accueil{% endblock %}

{% block body %}
    <h1>Accueil</h1>
    <strong>ici il y aura des infos trop bien</strong> <br>

    <a href="{{ path('add-User') }}">Créer un utilisateur</a><br>
    <a href="{{ path('list-Users') }}"> Voir les utilisateurs</a><br>
    <a href="{{ path('add-Alliance') }}"> Créer une alliance</a><br>
    <a href="{{ path('list-Alliances') }}"> Voir les alliances</a>

    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
{% endblock %}